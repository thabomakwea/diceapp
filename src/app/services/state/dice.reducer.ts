// import { Action, createReducer, on } from '@ngrx/store';

// import { DiceState } from '../types/ways-to-call-state.interface';
// import { actions } from '.';

// const reducer = createReducer(
//   { loading: true } as WaysToCallState,
//   on(actions.fetchPageDataSuccess, (state, { sections }) => ({ ...state, sections, loading: false }))
// );

// export function diceReducer(state: WaysToCallState | undefined, action: Action): WaysToCallState {
//   return reducer(state, action);
// }
