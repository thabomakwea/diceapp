// import { createFeatureSelector, createSelector } from '@ngrx/store';

// import { DiceState } from '../types/ways-to-call-state.interface';

// const getState = createFeatureSelector<DiceState>('waysToCall');

// export const banner = createSelector(getState, (state: WaysToCallState) => state.sections?.['connect-more']);

// export const stayInTouch = createSelector(getState, (state: WaysToCallState) => state.sections?.['so-many-reasons-to-stay-in-touch']);

// export const makeTheCall = createSelector(getState, (state: WaysToCallState) => state.sections?.['make-call']);

// export const onYourTerms = createSelector(getState, (state: WaysToCallState) => state.sections?.['talk-your-terms-these-calling-services']);

// export const dropMessage = createSelector(getState, (state: WaysToCallState) => state.sections?.['drop-them-a-message']);

// export const helpAndSupport = createSelector(getState, (state: WaysToCallState) => state.sections?.['help-topics-ways-to-call']);

// export const loading = createSelector(getState, (state: WaysToCallState) => state.loading);
